// 1 Crie uma arrow function que receba dois números e retorne a soma deles.

const adicao = (numero1, numero2) => {
    return numero1 + numero2
}
const soma = adicao(2,5)
console.log(`A soma dos valores passados é = ${soma}`)